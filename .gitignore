# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Build
dist/
build/

# Turbo
.turbo/

# Environment
.env
.env.local
.env.*.local

# IDE / tooling
.vscode/
.idea/
.claude/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*

# Supabase
supabase/.temp/

# Original legacy code
original-code/

# Import & migration data (sensitive — contains member PII, IPs, password hashes)
# These are legacy Zone Nordiques exports and migration artifacts. They must
# never reach the repo: log_acces.csv alone holds member IDs + raw IP addresses
# (personal data under Québec Law 25 / GDPR).
membre.csv
chronique.csv
log_acces.csv
active-ids.txt
import-results.log
import-chroniques.log
fix-authors.log
*.import.log

# Migration run artifacts: ID maps (member/comment/article — member PII under
# Law 25), run logs and .out captures. Regenerated by the import scripts; the
# supabase/migrations/ files remain the schema source of truth.
/migration/
# Legacy Zone Nordiques DB dump (password hashes, raw IPs — never commit).
/zonenordiques_old/
# Generated full-schema dumps (redundant with supabase/migrations/).
/supabase/scripts/full_schema*.sql
